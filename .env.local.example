# Copie para .env.local: cp .env.local.example .env.local
# Reinicie o Vite após criar/alterar (.env.local sobrescreve .env).

VITE_BACKOFFICE_API_URL=http://localhost:8080

# MinIO local (bucket backoffice) — uploads em dev conteinerizado
VITE_R2_PUBLIC_BASE_URL=http://localhost:9000/backoffice
